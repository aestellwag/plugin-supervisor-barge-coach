{
  "name": "barge-coach",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "twilio-run --env",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "twilio-flex-token-validator": "^1.5.6"
  },
  "devDependencies": {
    "twilio-run": "^3.2.1"
  },
  "engines": {
    "node": ">=12"
  }
}
